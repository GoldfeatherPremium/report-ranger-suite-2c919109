# --- Required ---
SUPABASE_URL=https://qxftygfzqouzhznnmrsq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=paste-service-role-key-here

# --- Worker identity (must be unique per process) ---
WORKER_ID=contabo-1

# --- Tuning ---
HEADLESS=true
SUBMISSION_TIMEOUT_MS=900000    # 15 min wait for similarity score
UPLOAD_TIMEOUT_MS=600000        # 10 min for file upload + review screen to appear
POLL_INTERVAL_MS=30000          # refresh similarity page every 30 s
CLAIM_IDLE_MS=10000             # how long to sleep when no job/slot available
HEARTBEAT_MS=30000
