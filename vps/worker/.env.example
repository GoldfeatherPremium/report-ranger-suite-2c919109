# --- Required ---
SUPABASE_URL=https://qxftygfzqouzhznnmrsq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=paste-service-role-key-here

# --- Worker identity (must be unique per process) ---
WORKER_ID=contabo-1

# --- Tuning ---
HEADLESS=true
SUBMISSION_TIMEOUT_MS=1800000   # 30 min wait for similarity score
UPLOAD_TIMEOUT_MS=360000        # 6 min wait for the file upload to finish processing
POLL_INTERVAL_MS=15000          # how often to refresh the similarity page
CLAIM_IDLE_MS=10000             # how long to sleep when no job/slot available
HEARTBEAT_MS=30000
